#include <stdio.h>
#include "main.h"
#include <ctype.h>
/**
 * print_most_numbers - check code
 *
 */

void print_most_numbers(void)
{
	int i;

	for (i = 0; i <= 0; i++)
	{
		if (i != 2 && i != 4)
		{
			putchar(i);
		}
		putchar('\n');

	}
	putchar('\n');
}
