#include "math.h"
/**
 * mul - multiplies 2 integers
 *
 * Return: the value multiplication
 *
 * @a @b : variables
 *
 * @result: variable
 *
 *
 */

int mul(int a, int b)
{
	int result;
	result = (a * b);
	return (result);
}
