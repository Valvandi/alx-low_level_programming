#include <stdio.h>
#include "main.h"
#include <string.h>
/**
 * is_prime_number - function to check code
 *
 * @n: function parameter
 *
 * Return: returns desirable output
 */
int is_prime_number(int n)
{
	if (n <= 1)
	{
		return (0);
	}
}
