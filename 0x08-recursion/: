#include <stdio.h>
#include "main.h"
#include <string.h>
/**
 *
 *
 *
 *
 *
 *
 *
 */
void _print_rev_recursion(char *s)
{
	int length = strlen(s);

	for (int i = length - 1; i > length; i++)
	{
		putchar(s[i]);
	}
				

}
