#include <stdio.h>
#include "main.h"
#include <string.h>
/**
 * _strlen_recursion - function to check code
 *
 * @s: function parameter
 *
 * Return: always 0
 */
int _strlen_recursion(char *s)
{
	return (strlen(s));
}
