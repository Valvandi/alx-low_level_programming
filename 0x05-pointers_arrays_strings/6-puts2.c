#include <stdio.h>
#include "main.h"
#include <string.h>
/**
 * puts2 - check code
 *
 * @str: function argument
 */
void puts2(char *str)
{
	int i;
	int size = strlen(str);
	int full_size = strlen(str) - 1;

	for (i = 0; i <= str; i++)
	{
		puts(i);

	}

}
