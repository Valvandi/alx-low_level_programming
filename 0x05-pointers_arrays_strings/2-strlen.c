#include <stdio.h>
#include "main.h"
#include <string.h>
/**
 * _strlen- check len of string
 *
 * Return: always 0
 *
 *
 */
int _strlen(char *s)
{
	int len;

	len = strlen(*s);
	return (len);

}



