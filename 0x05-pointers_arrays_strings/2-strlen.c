#include <string.h>
#include <stdio.h>
#include "main.h"
/**
 * _strlen - check code
 *
 * @s: function argument
 *
 * Return: always 0
 */
int _strlen(char *s)
{
	int len = strlen(s);

	printf("%d", len);

	return (0);
}
